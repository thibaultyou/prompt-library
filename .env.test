# Test Environment Variables
# This file is used for test environment only

# Critical Settings
ANTHROPIC_API_KEY=test_anthropic_api_key
OPENAI_API_KEY=test_openai_api_key
LOG_LEVEL=error
REMOTE_REPOSITORY=https://github.com/test/repo

# User Preferences 
# These should only be used as fallbacks if no config exists
MODEL_PROVIDER=anthropic
ANTHROPIC_MODEL=claude-test
ANTHROPIC_MAX_TOKENS=4000
OPENAI_MODEL=gpt-test
OPENAI_MAX_TOKENS=4000
PROMPT_FILE_NAME=prompt.md
METADATA_FILE_NAME=metadata.yml