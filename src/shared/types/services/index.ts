/**
 * Centralized re-export for all types related to service interfaces,
 * particularly those defining contracts for how different parts of the
 * application interact (e.g., commands interacting with UI services).
 */

export * from './command'; // Exports CommandInterface, ActionableCommand, ConfirmOptions
export * from './menu'; // Exports MenuCommand
